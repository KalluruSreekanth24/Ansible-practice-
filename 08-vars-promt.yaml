- name: loadnig variables in prompt
  hosts: frontend
  become: yes
  vars_prompt:
    - name: username
      prompt: what is your username?
      private: false
    - name: pass
      prompt: what is your pass?
      private: false  
  tasks:
    - name: print the following
      ansible.builtin.debug:
        msg: "hello, username is {{ username }}"